#include <stdio.h>
#include <math.h>
int main()
{
    char a[10];
    scanf("%s", a);
    /*
    gets(a);
    
    for (int i = 0; i < 10; i++)
    {
        printf("%c  ->  ", a[i]);
    }
    */
    printf("\n");
    puts(a);
    system("pause");
}