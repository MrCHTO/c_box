#include <stdio.h>
#include <math.h>
#define float double
void main()
{
    /*考生在此设计程序*/
    float a = 0, b = 0, s = 0, i, n;
    for (n = 0; n <= 9; n++)
    {
        if (i = 2 * n)
        {
            a = pow(3, i) / (2 * i + 1);
        }
        if (i = 2 * n + 1)
        {
            b = pow(3, i) / (2 * i + 1);
        }
    }
    s = s + a - b;
    printf("%.5f", s);
    system("pause");
}n
